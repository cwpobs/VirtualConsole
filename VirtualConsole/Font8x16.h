#pragma once

#include <cstdint>

// Зашитый в код битмап-шрифт 8x16, классический ROM-формат VGA: на
// каждый из 256 кодов CP866 (см. TextVRAM.cpp, CP866_HIGH) - 16 байт,
// один байт на строку глифа, бит 7 (старший) = крайний левый пиксель,
// 1 = закрашен. Никаких файлов на диске не нужно - VideoConsole
// работает с этим массивом напрямую (см. ASSEMBLY.md, "VideoConsole").
// Индекс совпадает с байтом CP866 в VRAM - перекодировка не нужна.
//
// Данные - настоящий CP866 8x16 VGA-дамп (см. Font8x16.cpp), а не
// заглушка.
extern const uint8_t FONT_8X16[256 * 16];
